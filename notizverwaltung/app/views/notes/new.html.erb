<%= form_for :note, url: notes_path do |f| %>
	<div class="form-group">
		<%= f.text_field :heading, class: "form-control"%>
   		<%= f.text_area :message, class: "form-control", rows:"15" %>

   		   	<p>
   	<%= f.label :due_at %>
   	<%= f.text_field :due_at, :placeholder => t('placeholder_due_at') %>
   	</p>

    	<%= f.submit "Speichern" %>
    	<%= button_to "Zurück", notes_path, :class => "inbox-sf-add-btn tip", :method => :get %>
  	</div>

<!--%= f.collection_select(:note, :category_id, Author.all, :id, :name_with_initial, prompt: true) %-->


<% end %>