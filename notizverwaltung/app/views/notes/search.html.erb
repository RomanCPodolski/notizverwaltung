<h2>Such Ergebnis<h2>

<ul>
	<% @notes.each do |note| %>
  		<li><%= link_to note.message, :action => 'edit', :id => note.id  %></li>
  	<% end %>
</ul>
<p>
	<%= button_to "Zurück", notes_path, :class => "btn btn-default", :method => :get %>
</p>