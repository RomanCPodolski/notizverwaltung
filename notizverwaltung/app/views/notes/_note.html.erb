<tr>
	<td><%= note.id %></td>
	<td><%= note.message %></td>
	<td><%= button_to "löschen", note_path(note), :id => "", :class => "inbox-sf-add-btn tip", method: :delete, data: { confirm: 'wirklich löschen?' } %></td>
	<td><%= button_to "bearbeiten", edit_note_path(note), :class => "inbox-sf-add-btn tip", method: :get %></td>
</tr>